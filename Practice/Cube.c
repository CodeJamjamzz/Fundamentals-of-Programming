#include <stdio.h>

int main (){

        int input, i = 1, cube, num;

    printf("Enter a number ");
    scanf("%d", &input);

        while(i <= input){
            printf("\nNumber is: %d and cube of the %d is %d", i, i, cube, cube = i*i*i);
            i++;
        }
    
}